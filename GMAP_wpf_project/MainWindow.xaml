<Window x:Class="GMAP_wpf_project.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GMAP_wpf_project"
        mc:Ignorable="d"
        xmlns:gmap="clr-namespace:GMap.NET.WindowsPresentation;assembly=GMap.NET.WindowsPresentation"
        Title="MainWindow" Height="450" Width="800"
        Closing="Window_Closing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="150"/>
        </Grid.RowDefinitions>

        <!-- 컨트롤 패널 -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="10">
            <StackPanel Orientation="Horizontal">
                <Label Content="시리얼 포트:" Foreground="White" VerticalAlignment="Center"/>
                <ComboBox x:Name="ComboBoxPorts" Width="100" Margin="5,0"/>
                <Button x:Name="ButtonConnect" Content="연결" Width="60" Margin="5,0" Click="ButtonConnect_Click"/>
                <Button x:Name="ButtonDisconnect" Content="해제" Width="60" Margin="5,0" Click="ButtonDisconnect_Click"/>

                <Separator Width="20"/>

                <Label Content="상태:" Foreground="White" VerticalAlignment="Center"/>
                <Label x:Name="LabelStatus" Content="연결안됨" Foreground="Red" VerticalAlignment="Center"/>

                <Separator Width="20"/>

                <Button x:Name="ButtonClearMap" Content="지도 초기화" Width="80" Margin="5,0" Click="ButtonClearMap_Click"/>
                <Button x:Name="ButtonSimulate" Content="시뮬레이션" Width="80" Margin="5,0" Click="ButtonSimulate_Click"/>
            </StackPanel>
        </Border>

        <!-- 구글맵 -->
        <gmap:GMapControl x:Name="MainMap" 
                          Grid.Row="1" 
                          Zoom="13" 
                          MinZoom="2" 
                          MaxZoom="20"
                          MouseWheelZoomType="MousePositionAndCenter"
                          CanDragMap="True"
                          ShowTileGridLines="False"/>

        <!-- 로그 패널 -->
        <Border Grid.Row="2" Background="#34495E" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>

                <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="TextBlockLog" 
                               Foreground="White" 
                               FontFamily="Consolas" 
                               FontSize="12"
                               TextWrapping="Wrap"/>
                </ScrollViewer>

                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <Label Content="수신된 데이터:" Foreground="White" FontWeight="Bold"/>
                    <Label x:Name="LabelLatitude" Content="위도: -" Foreground="LightGray"/>
                    <Label x:Name="LabelLongitude" Content="경도: -" Foreground="LightGray"/>
                    <Label x:Name="LabelDataCount" Content="데이터 개수: 0" Foreground="LightGray"/>
                    <Label x:Name="LabelLastUpdate" Content="마지막 업데이트: -" Foreground="LightGray"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
